# Issues Index

Every issue that exists or ever existed in this directory. Statuses:
`active` (tracked, open), `closed` (archived on disk with the matching
filename suffix, untracked).
For archived entries, "Last tracked" is the commit still containing the
tracked file: `git show <hash>:docs/issues/<file>.md` retrieves it.

| Issue | Status | Last tracked | Description |
| --- | --- | --- | --- |
| `container-demuxers-register-video-suffixes-that-are-not-candidates.md` | active | - | `VIDEO_EXTENSIONS` holds a partial subset of what the corresponding demuxers register. The two raw elementary stream demuxers are now complete, because every suffix `h264` and `hevc` register denotes a raw video stream. The container demuxers cannot be taken the same way: of the fourteen suffixes they register and the set lacks, seven are video (`.3gp`, `.3g2`, `.f4v`, `.ismv`, `.psp`, `.mj2`, `.mk3d`) and seven are not -- four audio spellings, a subtitle form, a still image (`.avif`, mime type `image/avif`), and `.ism`, which is a manifest rather than media. So a full-list rule that is correct for the raw codecs admits audio, subtitles, an image and a manifest when applied to containers. Registration is not the authority either: five current members (`.mts`, `.m2ts`, `.mpg`, `.mpeg`, `.wmv`) are registered by no demuxer at all and are reached by content probing. Deferred because none of the seven has been observed: the set records what this stack has actually been handed, which is why `.h264` is in it and why five members are there with no registration behind them. |
| `test-stubs-drift-from-the-signatures-they-stand-in-for.md` | closed | dbdb702 | A stand-in installed over a real callable is written against the signature of the day, and nothing connects the two afterwards: a delegating stub stops measuring what it was installed to measure and raises `TypeError` about the argument shape instead, and a raising stub meant to prove a path is never reached reports that same `TypeError` rather than its own cause. A `**_keywords` wildcard makes it worse by accepting calls the original rejects. Scoped to signature parity -- names, kinds and defaults -- not to what a stub asserts. Confirmed in the ffprobe, convert, hwaccel, seek-landing and reader-error suites; the population is a property, not a list, so the document carries the recipe that locates it. Resolved: every stand-in mirrors its original's names, kinds and defaults; no wildcard remains; the one delegating stand-in forwards what it accepts; and the one swap that could become a subclass override did, where the type checker holds it permanently. Unread parameters are deleted rather than renamed, since an underscore prefix satisfies the unused-parameter check while breaking the keyword binding. Only that override is mechanically pinned -- the attribute swaps are mirrored by hand and can drift again. |
| `sparse-reads-undercount-the-sequential-delivery-count.md` | closed | 899fc0a | `read_frames` calls `seek`, which zeroes the delivery counter, but delivers through `_read_current`, which never increments it -- so a sequential read following a sparse one reports its shortfall low by the frames the sparse read yielded. Reachable with honest facts on the very source class the backstop exists for. The error still fires and no frame is misdelivered; only the count is wrong. Resolved: `read_frames` rebases the delivery count onto the cursor it leaves, so the sequential phase counts grid slots from where it resumes, pinned on both an unstrided and a strided window. Filed as deferred on the reasoning that any fix reaches the sparse cursor contract; measured, it does not. The deferral's other worry, that a strided window breaks the arithmetic, was correct and shaped the fix. |
| `context-managers-annotate-the-concrete-class-not-self.md` | closed | 899fc0a | All three io context managers annotated `__enter__` with a quoted concrete class name rather than `Self`, so a subclass lost its own type inside a `with` block and any attribute it added failed the strict type check with no suppression permitted. Already paid for once: a test observing decoder state subclasses `VideoReader` and had to abandon `with` for `try`/`finally`. Resolved: all three return `Self`, and a test subclass reaches its own methods through a `with` block; annotation-only, no behavior change, and no floor change at 3.12. |
| `ffmpeg-runner-duplication.md` | closed | c09a2e2 | Consolidate the per-module run-ffmpeg-to-completion helpers into one core leaf. Resolved: `mosaic_media/ffmpeg.py` owns the run-to-completion helper for the three blocking sites, the shared output postcondition, and the failure wording all four sites use. The document's four-site premise was stale -- the converter's Popen progress lifecycle shares the vocabulary, not the mechanism, and keeps its own runner. |
| `thumbnail-dimension-tests-assert-literal-tuples.md` | closed | c4887ef | Rewrite the thumbnail_dimensions tuple-equality assertions as property assertions. Resolved: a shared contract helper asserts the guarantees that survive any rounding rule (long edge pinned to cap, short edge at least 1, orientation preserved, long-to-short ratio within the tolerance a pixel of rounding allows), across both orientations and a non-default cap, and one separately named test pins the current rounding so a change to it fails there alone. The short-edge assertion this document proposed was itself a restatement of the implementation formula and was not adopted. |
| `seek-index-counts-duplicate-timestamp-packets.md` | closed | 5e65eb0 | Deduplicate presentation timestamps in build_seek_index consistently with measure_timing so VP8/VP9 alternate-reference packets cannot shift keyframe ranks and reintroduce off-by-N seeks. |
| `writer-h264-path-depends-on-gpl-libx264.md` | implemented | - | `FFmpegVideoWriter` encoded through `libx264` (GPL-2.0-or-later), a link-time GPL dependency inherited by every process importing this package, because PyAV links FFmpeg in-process. Resolved: the writer encodes AV1 via `libsvtav1` and `av1_nvenc`, the generated corpus is AV1, H.264 test media is committed under `tests/assets/`, and `tests/test_encoder_guard.py` keeps any GPL encoder name out of `src/` and `tests/`. The document's Scope section was wrong on one point: it claimed the writer had no caller beyond its own tests, and two call sites did use it, which is why the x264-scale arguments were kept as a translated, deprecated path rather than removed. (Never tracked; the durable record is this row, the README License section, and the on-disk `.implemented.md`.) |
| `raw-stream-remux-relies-on-a-deprecated-muxer-fallback.md` | closed | dbdb702 | A raw elementary stream that states no frame rate in its sequence parameter set has nothing for the probe to read, so its copy remux still falls back to `-fflags +genpts` and the mp4 muxer's deprecated timestamp synthesis; measured, the notice still fires for that class. The common case is resolved -- a stream that states a rate gets timestamps this package writes, pinned by a test with a companion proving that check can fail. Two claims this document made were disproved: there is no FFmpeg 7.0 boundary, and the remuxed file never carried 25 fps timestamps. Closing the remainder is a design question, not a missing edit: nothing in such a file states a rate. Resolved: a source stating no rate in either its container or its bitstream is refused ahead of every reason, on both targets, rather than receiving the muxer's approximation. The committed fixture is HEVC rather than H.264 -- no encoder parameter omits H.264 timing and the metadata filter does not strip it -- and what it pins is codec-independent, since the refusal keys on the timing provenance and the declared rate. |
| `reordered-raw-streams-get-decode-order-timing.md` | closed | dbdb702 | A raw elementary stream carries no timestamps, so a copy remux synthesizes them from the packet index -- decode order. For a bitstream coded with frame reordering that is not presentation order, so the timestamps are shuffled relative to the pictures they label (measured: the fourth frame carries a lower timestamp than the second), and the acceptance re-probe cannot see it because they remain uniform and complete. Not a regression; the preceding muxer fallback did the same. Closing it means routing such a source to a re-encode, which costs a persisted-fact schema change, a new verdict reason, and a change wherever a reason set is enumerated rather than passed through. Not yet observed: the Baseline profiles forbid B-slices and no raw stream seen so far is reordered, but a Main or High profile device can be. The document records the generating recipe so the measurement can be rechecked. Resolved: the coded reordering depth is a fact, `presentation_timing_requires_decode` is in both vocabularies and both re-encode sets, and both copy remuxes give way to a re-encode. Measured on the generated fixture, a copy remux produced 17 non-monotonic pairs and the re-encode zero. The committed-fixture bullet is met by a deterministic stream copy of a committed asset rather than a separately committed file, and the consumer bullet by measurement: nothing branches on an individual reason, so a new literal is additive. |
| `encoder-gate-checks-listing-not-usability.md` | closed | 8cb3da8 | Both encode-side consumers gated hardware on `ffmpeg -encoders` listing the name, a build-time fact, so permission on a machine whose device cannot run the encoder selected one that failed at startup rather than falling back. The writer half resolved first, in process, by opening an `av1_nvenc` CodecContext. Resolved: `hwaccel.encoder_usable` opens the named encoder and encodes one frame for the transcode path, which runs system ffmpeg as a subprocess and so cannot be answered by the in-process probe, and `_selected_encoder` takes hardware only on permission AND that check. Two departures from the plan this document carried. The probe does not initialize a CUDA device: Pascal initializes CUDA perfectly and has no AV1 encoder, so that form answers True on exactly the reported host, and its frame is 256x256 because NVENC's per-codec minimum encode resolution is largest for AV1 and the decode probe's 64x64 would report False on a card that encodes AV1 correctly. `encoder_available` kept its signature and gained a sibling rather than changing in place, because `requires_svtav1` calls it at collection time and would otherwise run a device probe for a CPU encoder. The encoder each run used is now recorded on the command and the result, since the operation and the output codec read identically whichever ran. |
| `multi-video-open-pays-per-file-probe.md` | closed | e3179b8 | Add a facts-injection seam to MultiVideoReader so construction stops paying a per-file ffprobe subprocess the callers' held MediaFacts already answer; junction report bounded at 1.5x meanwhile. Resolved: the constructor accepts facts= and indices=, and the injected junction workload is gated at the carve tier (measured 1.065). |
| `run-transcode-lacks-progress-and-cancellation.md` | closed | 7920acd | Give run_transcode optional on_progress and cancel_check parameters over a Popen `-progress pipe:1` invocation (fraction against facts.duration, None when duration is 0), preserving temp cleanup and timeout -- the upstream dependency of a long-running transcode job. Resolved: the callbacks thread through a reader-thread run loop that maps each progress block to a TranscodeProgress and polls the cancel token, raising a TranscodeError that names a canceled run. |
| `encoding-presets-unmeasured-against-quality-goals.md` | active | - | Measure both encoding presets on a representative set of real footage (static and moving cameras, small and large subjects, fast movement, variable backgrounds) against their goals -- analysis near lossless, playback visually lossless -- pick the crf values from the measurements, and decouple the NVENC cq value from the SVT-AV1 crf, whose scales are not equivalent. |
| `mosaic-api-finalize-strips-media-index-columns.md` | closed | - | An upload finalize step elsewhere rewrote its media index with only the pre-migration columns, dropping the seven a separate indexing step writes. Resolved where it lived; nothing in this repository changed. (Never tracked; the substance lives in the on-disk `.closed.md`. Its filename names another codebase and predates the rule against that, and it stays as written because the filename is how the archived document is found.) |
| `playback-transcode-introduces-non-zero-start-time.md` | closed | 899fc0a | Transcode command construction was not checked against what the output must satisfy: a playback run introduced the non-zero start time its own probe rejects, and an analysis run copied VP8 into a container that cannot carry it. Resolved: `_select_operation` escalates a copy to a re-encode when the output container cannot carry the source codec, and when the copy would carry forward packets the source itself does not decode, so a source cut mid-GOP is re-encoded rather than rewrapped and its derivative starts at zero carrying every frame. Read the diagnosis with the correction in `docs/specs/2026-08-01-deliverable-frame-model.md`: the shift is the dropped leading prefix, not an edit-list composition offset acquired at the muxer. |
| `reader-cannot-deliver-frames-the-facts-declare.md` | closed | 899fc0a | The reader raised or returned nothing for frame indices inside the count the probe declares. Resolved in the terms the document set: every index inside `frame_count` either returns the frame belonging to it or raises an error naming that index. The reader delivers the leading-packet class through the frames-preserving decoder flag scoped to a decode segment starting at the stream start, and the edit-list class by ignoring an edit list measured to drop frames; the null-frame class is routed to a derivative that carries them, and a caller reading such a source directly gets the per-frame delivery check's named error. Read the diagnosis with the correction in `docs/specs/2026-08-01-deliverable-frame-model.md`: the `Operation not permitted` text carries no meaning, being libav's bare `-1` for a backward seek below a container index that begins 1.8 s in. |
| `duplicate-timestamps-mislabel-a-factless-sequential-read.md` | closed | dbdb702 | A container carrying two frames at one presentation timestamp makes the decoder emit more frames than the frame model counts, so every index after the duplicate is one behind the picture it names. The indexed paths raise (frame 11 sequentially, frame 15 on a seek); a sequential read holding facts but no index delivers 60 of 60 with 49 labels wrong and no error. The decode-gap check cannot reach it under any threshold, because a duplicated frame collapses the spacing to 0.0 rather than widening it. Not a regression -- every path mislabeled silently before the delivery checks. Two decoded pictures at one timestamp still has no file: the mp4 muxer refuses duplicate decode timestamps outright, and VP8/VP9 alternate-reference packets do not produce it. The packet-layer duplicate does have one, and it settles the deduplication's premise empirically for the first time: an AV1 encode split by `av1_frame_split` is benign in Matroska (74 packets, 50 timestamps, 50 pictures, reads and seeks clean) and is the null-frame shape as a bare stream (74 timestamps, 50 pictures, raises at frame 1), so a check comparing packet count against distinct timestamp count fires on the healthy file and stays silent on the broken one. Resolved: the reader raises on exact equality between consecutive decoded timestamps, ahead of the spacing guards and behind the frame-rate guard, at zero packet scans. Equality rather than a non-positive spacing, because a backwards step is the reordering defect the verdict now routes to a re-encode. The frame-model question the document poses did not need answering: the collapse is visible in a quantity the spacing check already computed and discarded. |
