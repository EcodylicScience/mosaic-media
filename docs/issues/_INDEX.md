# Issues Index

Every issue that exists or ever existed in this directory. Statuses:
`active` (tracked, open), `closed` (archived on disk with the matching
filename suffix, untracked).
For archived entries, "Last tracked" is the commit still containing the
tracked file: `git show <hash>:docs/issues/<file>.md` retrieves it.

| Issue | Status | Last tracked | Description |
| --- | --- | --- | --- |
| `reconstructed-facts-always-claim-measured-timing.md` | active | - | `timing_measured` defaults to `True` for backward compatibility, so a consumer rebuilding `MediaFacts` from persisted columns without that field asserts measured timing on every row, including raw elementary streams whose timing is unmeasurable. `compare_for_duplicate` guards its division on `duration > 0.0` rather than on the flag, so the crash is closed; the misrepresentation is not. Closing it needs the consumer to persist and restore the field. |
| `transcode-provenance-has-no-consumer-column.md` | active | - | `TranscodeResult.source_video_uuid` is minted and then dropped: unlike `video_uuid` and `content_digest`, which round-trip inside the serialized `MediaFacts` blob with no schema change, it belongs to neither the derivative's facts nor its verdict, so a consumer flattening a transcode into its media index has no column for it. It is the move-resilient form of an edge the consumer already stores as `source_path`, so what is lost until then is resilience to a rename, not the provenance itself. Closing it needs one consumer-side column, and existing rows backfill by following `source_path` to the source row's `video_uuid`; nothing changes in this repository. |
| `ffmpeg-runner-duplication.md` | closed | c09a2e2 | Consolidate the per-module run-ffmpeg-to-completion helpers into one core leaf. Resolved: `mosaic_media/ffmpeg.py` owns the run-to-completion helper for the three blocking sites, the shared output postcondition, and the failure wording all four sites use. The document's four-site premise was stale -- the converter's Popen progress lifecycle shares the vocabulary, not the mechanism, and keeps its own runner. |
| `thumbnail-dimension-tests-assert-literal-tuples.md` | closed | c4887ef | Rewrite the thumbnail_dimensions tuple-equality assertions as property assertions. Resolved: a shared contract helper asserts the guarantees that survive any rounding rule (long edge pinned to cap, short edge at least 1, orientation preserved, long-to-short ratio within the tolerance a pixel of rounding allows), across both orientations and a non-default cap, and one separately named test pins the current rounding so a change to it fails there alone. The short-edge assertion this document proposed was itself a restatement of the implementation formula and was not adopted. |
| `seek-index-counts-duplicate-timestamp-packets.md` | closed | 5e65eb0 | Deduplicate presentation timestamps in build_seek_index consistently with measure_timing so VP8/VP9 alternate-reference packets cannot shift keyframe ranks and reintroduce off-by-N seeks. |
| `writer-h264-path-depends-on-gpl-libx264.md` | implemented | - | `FFmpegVideoWriter` encoded through `libx264` (GPL-2.0-or-later), a link-time GPL dependency inherited by closed-source consumers because PyAV links FFmpeg in-process. Resolved: the writer encodes AV1 via `libsvtav1` and `av1_nvenc`, the generated corpus is AV1, H.264 test media is committed under `tests/assets/`, and `tests/test_encoder_guard.py` keeps any GPL encoder name out of `src/` and `tests/`. The document's Scope section was wrong on one point: it claimed the writer had no consumer beyond its own tests, and two `mosaic` call sites did use it, which is why the x264-scale arguments were kept as a translated, deprecated path rather than removed. (Never tracked; the durable record is this row, the README License section, and the on-disk `.implemented.md`.) |
| `ffmpegvideowriter-callers-pass-x264-scales.md` | active | - | Two `mosaic` call sites still pass `FFmpegVideoWriter`'s deprecated x264-scale `crf` and `preset`; the writer translates them onto SVT-AV1's scales so the output is correct, but every call warns and the two numeric scales stay one mistake apart (23 is a middling x264 rate and a near-lossless AV1 one). Closing it moves both call sites to `av1_crf` / `av1_preset` with values from the presets measurement, after which the deprecated parameters can be retired. |
| `raw-stream-remux-relies-on-a-deprecated-muxer-fallback.md` | active | - | `REMUX_TIMEBASE` gives a raw H.264 elementary stream the timestamps a container carries. From FFmpeg 7.0 `-fflags +genpts` no longer generates them at the demuxer, so the copy relies on the mp4 muxer's own fallback, which ffmpeg logs as deprecated. Output is correct on every targeted version including the pinned 8.1, and `-v error` hides the message, but the raw-stream path breaks outright when that fallback is removed. Four alternatives were measured; none sets demuxer-side timestamps without misdeclaring the rate. |
| `encoder-gate-checks-listing-not-usability.md` | active | - | Gate hardware encoding on a usability-checked probe (mirroring the nvdec device-init fix) in both the writer and the transcode command builder, falling back to CPU encoders when the device is unusable. |
| `multi-video-open-pays-per-file-probe.md` | closed | e3179b8 | Let the consumer migration add a facts-injection seam to MultiVideoReader so construction stops paying a per-file ffprobe subprocess the callers' held MediaFacts already answer; junction report bounded at 1.5x meanwhile. Resolved: the constructor accepts facts= and indices=, and the injected junction workload is gated at the carve tier (measured 1.065). |
| `run-transcode-lacks-progress-and-cancellation.md` | closed | 7920acd | Give run_transcode optional on_progress and cancel_check parameters over a Popen `-progress pipe:1` invocation (fraction against facts.duration, None when duration is 0), preserving temp cleanup and timeout -- the upstream dependency of the mosaic transcode job. Resolved: the callbacks thread through a reader-thread run loop that maps each progress block to a TranscodeProgress and polls the cancel token, raising a TranscodeError that names a canceled run. |
| `encoding-presets-unmeasured-against-quality-goals.md` | active | - | Measure both encoding presets on a representative set of real footage (static and moving cameras, small and large subjects, fast movement, variable backgrounds) against their goals -- analysis near lossless, playback visually lossless -- pick the crf values from the measurements, and decouple the NVENC cq value from the SVT-AV1 crf, whose scales are not equivalent. |
| `mosaic-api-finalize-strips-media-index-columns.md` | closed | - | mosaic_api upload finalize (write_index_rows / INDEX_COLUMNS) rewrote media_raw/index.csv with only the pre-migration columns, dropping the seven new media-index columns (media_facts, frame_count, analysis_transcode, stream_transcode, analysis_derivative_path, playback_derivative_path, source_path) that mosaic's index_media now writes. Resolved: mosaic_api's finalize.write_index_rows now tracks mosaic's MEDIA_INDEX_COLUMNS and preserves unknown columns; the coupling and fix are documented in migration-mosaic-api.md section 3.7. The mosaic mosaic-media-migration branch still merges in sync as a push gate. (Never tracked; substance lives in the guide and the on-disk .closed.md.) |
